# ⚡ SUPER DESKTOP

> **Повноцінний оверлей-простір для Hyprland / Omarchy зі стікі-нотатками та міні-вікнами AI-терміналів.**
> При натисканні гарячої клавіші `SUPER + SHIFT + Q` всі нотатки та AI-термінали плавно виїжджають з країв екрану, а при повторному натисканні — ховаються і повністю невидимі на стандартному робочому столі.

---

## 🌟 Основні можливості (Key Features)

### 1. 📝 Стікі-нотатки (Sticky Notes)
- **Миттєве редагування**: клацніть мишею в будь-яку нотатку та записуйте думки, завдання чи код.
- **Дизайн у стилі теми Omarchy**: оформлення карток нотаток повністю слідує активній темі Omarchy (кольори заголовка, акценти, фон та текст автоматично синхронізуються).
- **Створення нових нотаток**: натисніть кнопку `📝 + Note` у верхній панелі (або `Ctrl + N`).
- **Автозбереження**: всі координати, розміри та текст автоматично зберігаються в `~/.config/super-desktop/state.json`.


### 2. 💻 AI-термінали (Interactive Small Terminals & AI Agents)
- **Повноцінний інтерактивний міні-термінал**: картки шириною від 320px (`MIN_CARD_WIDTH = 320`) є повноцінними інтерактивними терміналами (на базі VTE4). Можна клікати всередину, вводити команди, запускати процеси, скролити та взаємодіяти з AI-агентом прямо у віконці оверлею без необхідності розгортати його на весь екран.
- **Іконіфікація (128×128) та розгортання**:
  - Кнопка `🗕` у заголовку перетворює вікно на компактну іконку 128×128 px з великою емблемою агента та індикатором статусу (VTE акуратно від'єднується для економії ресурсів).
  - Кнопка `🗖` (або подвійний клік по іконці) миттєво розгортає картку назад у її попередній більший розмір, обраний користувачем, та відновлює живий термінал.
- **Вільний ресайз із "привидом" (Ghost Resizing)**:
  - Тягніть нижній правий кут (`◢`) для довільної зміни розмірів.
  - Під час руху миші відображається пунктирний "привид" майбутнього розміру.
  - Якщо потягнути розмір менше 320px — "привид" показує перехід в режим `🗕 128 × 128 (Icon)`. При відпусканні миші картка стає іконкою.
  - Якщо потягнути більше 320px — "привид" показує `💻 W × H (Terminal)`. При відпусканні миші вікно стає живим інтерактивним терміналом нового розміру.
- **Підтримка AI-агентів**:
  - 🌌 **Antigravity CLI** (`agy` / `antigravity`)
  - ⚡ **Claude Code** (`claude`)
  - 🤖 **OpenAI Codex** (`codex`)
  - 🔮 **OpenCode** (`opencode`)
  - 🚀 **Grok CLI** (`grok`)
  - 🧭 **Reasonix** (`reasonix code`, або `npx -y reasonix code` без глобальної інсталяції)
  - 🧠 **Aider** (`aider`)
  - 💻 **Bash / Zsh Shell**
- **⚙ Вибір харнесів у верхній панелі**: кнопка `⚙ Settings` відкриває панель зі списком харнесів, які реально встановлені на цій машині (перевірка через `which` / `npx`). Позначте `ON` / `OFF`, щоб показати або сховати відповідну кнопку запуску у верхній панелі; вибір зберігається у `state.json` (`visible_harnesses`) і застосовується миттєво. За замовчуванням показано все, що знайдено на машині.
- **Живий статус**:
  - `● ACTIVE` (зелений індикатор) — агент працює або очікує вводу.
  - `● WORKING` (жовтий пульсуючий індикатор) — агент генерує відповідь, компілює або виконує задачу.
  - `○ EXITED` (сірий індикатор) — процес завершено.
- **Розгортання всередині оверлею**:
  - Подвійний клік по заголовку (або кнопка ⛶) розгортає термінал до **80% екрана**, зберігаючи сесію та підлаштовуючи розмір шрифту.
  - Повторний подвійний клік по заголовку (або 🗕) згортає термінал назад у компактне вікно на оверлеї.
  - Подвійний клік всередині терміналу виділяє слова (не конфліктує з розгортанням).
  - `SUPER + SHIFT + Q` ховає **весь** оверлей; усі сесії продовжують працювати у фоновому tmux.

### 3. 🪄 Оверлей та анімації
- **Невидимий на стандартному робочому столі**: коли прихований, жодне вікно, стікер чи термінал не займає місце на Hyprland воркспейсах.
- **Анімація виїзду зі сторін**: при виклику нотатки та термінали плавно ковзають з найближчих країв екрана (зліва, справа, зверху, знизу) на свої збережені позиції.
- **Ефект розмиття фону (Blur)**: інтеграція з Hyprland layer-shell розмиває робочий стіл позаду карток.
- **Кнопка ✨ Arrange**: автоматично вирівнює всі нотатки зліва в акуратні колонки, а термінали — справа.

### 4. 🎨 Повна інтеграція з темами Omarchy (Omarchy Theming)
- **Динамічна палітра**: кольори фону, панелей, кнопок, бейджів, рамок та акцентів витягуються безпосередньо з активної теми Omarchy (`~/.local/state/omarchy/current/theme/colors.toml`).
- **Синхронізація на льоту**: при перемиканні теми в системі (`omarchy theme set <назва>` або через графічний селектор) встановлений хук (`~/.config/omarchy/hooks/theme-set.d/super-desktop`) миттєво оновлює стиль SUPER DESKTOP без перезапуску демона.
- **Термінальна палітра VTE**: вбудований термінал VTE адаптує всі 16 ANSI-кольорів, колір фону, тексту, курсору та системний моноширинний шрифт (Adwaita Mono, JetBrains Mono) під поточну тему Omarchy.

---

## ⌨️ Гарячі клавіші (Keybindings)

| Комбінація | Дія |
|---|---|
| `SUPER + SHIFT + Q` | **Показати / сховати SUPER DESKTOP** (працює як в англійській, так і в українській розкладці) |
| `Esc` | Сховати SUPER DESKTOP |
| `Ctrl + N` | Створити нову нотатку |
| `Ctrl + T` | Створити новий термінал |
| **Подвійний клік на термінал** | Розгорнути / згорнути термінал всередині оверлею (80%) |

---

## 🛠️ Встановлення (Installation)

У папці проєкту запустіть інсталятор:

```bash
cd ~/GitHub/super-desktop
./install.sh
```

Інсталятор:
1. Створить симлінк `~/.local/bin/super-desktop`.
2. Зареєструє додаток у меню додатків (`~/.local/share/applications/super-desktop.desktop`).
3. Додасть прив'язку клавіш `SUPER + SHIFT + Q` до `~/.config/hypr/bindings.lua`.
4. Додасть правило розмиття `hl.layer_rule` до `~/.config/hypr/hyprland.lua`.
5. Встановить хук синхронізації тем `~/.config/omarchy/hooks/theme-set.d/super-desktop`.
6. Перезавантажить та перевірить конфігурацію Hyprland.

---

## 💻 Керування через командний рядок (CLI)

```bash
# Перемкнути видимість (показати/сховати)
super-desktop toggle

# Показати оверлей
super-desktop show

# Сховати оверлей
super-desktop hide

# Перевірити статус, кількість нотаток та активних агентів
super-desktop status

# Дізнатися поточну активну тему
super-desktop theme

# Примусово перечитати та оновити кольори теми
super-desktop reload-theme

# Створити нову нотатку з текстом
super-desktop add-note "Зробити рев'ю пуллреквесту"

# Запустити новий термінал для потрібного агента
super-desktop add-term claude
super-desktop add-term antigravity
super-desktop add-term codex
super-desktop add-term opencode
super-desktop add-term grok
super-desktop add-term reasonix
super-desktop add-term shell

# Зупинити фоновий демон
super-desktop kill

# Міст для OmarchyAILauncher: віддає sd_term_* сесії по LAN/Tailscale (порт 8759)
# Додатково анонсує себе в мережі як mDNS-сервіс _omarchy-harness._tcp
# (TXT: ver=1, host=<hostname>) через avahi — щоб застосунок на телефоні
# знаходив ноутбук без ручного вводу IP. Якщо avahi-utils немає, міст просто
# працює як раніше, а панель 📱 показує "not advertised — use the IP above".
super-desktop harness-bridge --port=8759

# Разовий JSON-дамп сесій без сервера (для дебагу)
super-desktop harnesses
```

---

## 🏗️ Архітектура проєкту (Pure Rust Native)

```
/home/gladimdim/GitHub/super-desktop/
├── bin/
│   └── super-desktop            # Виконуваний лаунчер з підтримкою LD_PRELOAD
├── src/
│   ├── main.rs                  # CLI диспетчер та Unix socket IPC сервер/клієнт
│   ├── theme.rs                 # Парсер кольорів та шрифтів Omarchy (~/.local/state/omarchy/...)
│   ├── styles.rs                # Динамічна генерація GTK4 CSS на основі активної теми
│   ├── window.rs                # Wayland LayerShell вікно, анімації, HUD, 240Hz frame clock
│   ├── sticky_note.rs           # Віджет стікі-нотатки з підтримкою drag & drop
│   ├── mini_terminal.rs         # Віджет AI-терміналу (128x128 іконка, ресайз, VTE embed)
│   ├── tmux.rs                  # Керування фоновими tmux сесіями для AI-агентів
│   └── state.rs                 # Збереження стану в JSON (~/.config/super-desktop/state.json)
├── Cargo.toml                   # Залежності Rust (gtk4, gtk4-layer-shell, vte4, serde)
├── super-desktop.desktop        # Десктоп-файл додатку
├── install.sh                   # Скрипт автоматичного збирання та встановлення
└── README.md                    # Документація
```
