# ⚡ SUPER DESKTOP

> **Повноцінний оверлей-простір для Hyprland / Omarchy зі стікі-нотатками та міні-вікнами AI-терміналів.**
> При натисканні гарячої клавіші `SUPER + SHIFT + Q` всі нотатки та AI-термінали плавно виїжджають з країв екрану, а при повторному натисканні — ховаються і повністю невидимі на стандартному робочому столі.

---

## 🌟 Основні можливості (Key Features)

### 1. 📝 Стікі-нотатки (Sticky Notes)
- **Миттєве редагування**: клацніть мишею в будь-яку нотатку та записуйте думки, завдання чи код.
- **Вільне переміщення**: захоплюйте верхню панель (grip `⋮⋮`) будь-якої нотатки та перетягуйте її в будь-яке місце екрана.
- **Палітра кольорів**: кнопка 🎨 дозволяє перемикати кольори (Sunny Yellow, Fresh Mint, Sky Blue, Rose Pink, Lavender Purple, Cyber Dark Glass).
- **Створення нових нотаток**: натисніть кнопку `📝 + Note` у верхній панелі або **двічі клацніть на вільне місце фону**.
- **Автозбереження**: всі координати, розміри та текст автоматично зберігаються в `~/.config/super-desktop/state.json`.

### 2. 🤖 Міні-вікна терміналів (AI Mini-Terminals)
- **Компактні превʼю-картки**: розміром ~290×185 px (приблизно в 5 разів більші за звичайні іконки).
- **Підтримка AI-агентів**:
  - 🌌 **Antigravity CLI** (`agy` / `antigravity`)
  - ⚡ **Claude Code** (`claude`)
  - 🤖 **OpenAI Codex** (`codex`)
  - 🔮 **OpenCode** (`opencode`)
  - 🚀 **Grok CLI** (`grok`)
  - 🧠 **Aider** (`aider`)
  - 💻 **Bash / Zsh Shell**
- **Живий статус**:
  - `● ACTIVE` (зелений індикатор) — агент працює або очікує вводу.
  - `● WORKING` (жовтий пульсуючий індикатор) — агент генерує відповідь, компілює або виконує задачу.
  - `○ EXITED` (сірий індикатор) — процес завершено.
- **Живий міні-консольний перегляд**: прямо на картці відображаються останні рядки виводу терміналу в реальному часі.
- **Подвійний клік на весь екран**:
  - Подвійний клік на будь-який міні-термінал відкриває його **на весь екран** у терміналі `foot` як повноцінний Omarchy додаток (`foot -F -a super-desktop-terminal tmux attach -t ...`).
  - Оверлей автоматично ховається, щоб ви могли одразу працювати в терміналі.
  - Коли ви відключаєтесь (`Ctrl-b d`) або закриваєте вікно — сесія **продовжує працювати у фоні через tmux** і залишається на вашому SUPER DESKTOP!

### 3. 🪄 Оверлей та анімації
- **Невидимий на стандартному робочому столі**: коли прихований, жодне вікно, стікер чи термінал не займає місце на Hyprland воркспейсах.
- **Анімація виїзду зі сторін**: при виклику нотатки та термінали плавно ковзають з найближчих країв екрана (зліва, справа, зверху, знизу) на свої збережені позиції.
- **Ефект розмиття фону (Blur)**: інтеграція з Hyprland layer-shell розмиває робочий стіл позаду карток.
- **Кнопка ✨ Arrange**: автоматично вирівнює всі нотатки зліва в акуратні колонки, а термінали — справа.

---

## ⌨️ Гарячі клавіші (Keybindings)

| Комбінація | Дія |
|---|---|
| `SUPER + SHIFT + Q` | **Показати / сховати SUPER DESKTOP** (працює як в англійській, так і в українській розкладці) |
| `Esc` | Сховати SUPER DESKTOP |
| `Ctrl + N` | Створити нову нотатку |
| `Ctrl + T` | Створити новий термінал |
| **Подвійний клік на фоні** | Створити нову стікі-нотатку на місці курсору |
| **Подвійний клік на термінал** | Відкрити термінал на весь екран (`foot`) |

---

## 🛠️ Встановлення (Installation)

У папці проєкту запустіть інсталятор:

```bash
cd ~/GitHub/super-desktop
./install.sh
```

Інсталятор:
1. Створить симлінк `~/.local/bin/super-desktop`.
2. Зареєструє додаток у меню додатків (`~/.local/share/applications/super-desktop.desktop`).
3. Додасть прив'язку клавіш `SUPER + SHIFT + Q` до `~/.config/hypr/bindings.lua`.
4. Додасть правило розмиття `hl.layer_rule` до `~/.config/hypr/hyprland.lua`.
5. Перезавантажить та перевірить конфігурацію Hyprland.

---

## 💻 Керування через командний рядок (CLI)

```bash
# Перемкнути видимість (показати/сховати)
super-desktop toggle

# Показати оверлей
super-desktop show

# Сховати оверлей
super-desktop hide

# Перевірити статус, кількість нотаток та активних агентів
super-desktop status

# Створити нову нотатку з текстом
super-desktop add-note --text "Зробити рев'ю пуллреквесту"

# Запустити новий термінал для потрібного агента
super-desktop add-term --agent claude
super-desktop add-term --agent antigravity
super-desktop add-term --agent codex
super-desktop add-term --agent opencode
super-desktop add-term --agent grok
super-desktop add-term --agent shell

# Зупинити фоновий демон
super-desktop kill
```

---

## 🏗️ Архітектура проєкту

```
/home/gladimdim/GitHub/super-desktop/
├── bin/
│   └── super-desktop            # Виконуваний bash-скрипт з підтримкою LD_PRELOAD
├── super_desktop/
│   ├── __init__.py
│   ├── main.py                  # CLI диспетчер та Unix socket IPC сервер/клієнт
│   ├── window.py                # Wayland LayerShell вікно, анімації, HUD
│   ├── sticky_note.py           # Віджет стікі-нотатки з підтримкою drag & drop
│   ├── mini_terminal.py         # Віджет міні-терміналу з живим прев'ю та статусом
│   ├── tmux_manager.py          # Керування фоновими tmux сесіями для AI-агентів
│   ├── state_manager.py         # Збереження стану в JSON (~/.config/super-desktop/state.json)
│   └── styles.py                # Стилі GTK4 CSS (glassmorphism, палітри, neon glow)
├── super-desktop.desktop        # Десктоп-файл додатку
├── install.sh                   # Скрипт автоматичного встановлення та налаштування
└── README.md                    # Документація
```
